# Normalize line endings in the repository to LF, whatever the contributor's platform does
# locally. Without this, a Windows checkout would reintroduce CRLF on the next commit.
* text=auto eol=lf

# Line-ending conversion would corrupt these.
*.ico binary
*.png binary
*.jpg binary
*.pdb binary
*.dll binary
*.exe binary
